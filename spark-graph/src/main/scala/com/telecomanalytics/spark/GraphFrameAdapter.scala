package com.telecomanalytics.spark

import com.telecomanalytics.domain._


object GraphFrameAdapter {

  def toGraphFrame(schema: GraphSchema): Unit = {
    // placeholder: later convert to GraphFrame
  }
}
