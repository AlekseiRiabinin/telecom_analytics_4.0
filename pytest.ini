[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --cov=spark
    --cov-report=html
    --cov-report=term-missing
    --tb=short
filterwarnings =
    ignore::DeprecationWarning
